function C = Interleave(A,B)
% """ Take Vector A and B, concatenate into Vector C" + ...
%     
%     """"
%     C = [A;B];
%     C = C(:)';

for i=1:length(A)
    C(2*i) = A(i);
    C(2*i+1) = B(i);
end
